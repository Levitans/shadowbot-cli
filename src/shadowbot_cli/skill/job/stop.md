# shadowbot-cli job stop

停止指定任务。

## 用法

    shadowbot-cli job stop --job-uuid <任务UUID>

## 选项

| 选项 | 说明 |
| --- | --- |
| `--job-uuid` | 要停止的任务 UUID |

## 相关命令

- `shadowbot-cli job get`：查询任务状态
- `shadowbot-cli job list`：查询任务列表
